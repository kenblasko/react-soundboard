import {
  keyPress, 
  editKeyBinding, 
  setKeyBinding,
  setIsPlaying,
  handleChange,
  addKeyBinding,
  removeKeyBinding,
} from './items'

import {
  onFileUpload,
} from './upload'

export default {
  keyPress,
  onFileUpload,
  editKeyBinding,
  setKeyBinding,
  setIsPlaying,
  handleChange,
  addKeyBinding,
  removeKeyBinding,
}